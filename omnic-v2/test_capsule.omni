// Capsule test - Business logic containers

capsule Estoque {
    flow reservar(id: i64) -> bool;
    flow consultar(id: i64) -> i64;
    flow liberar(id: i64) -> bool;
}

capsule Pedidos {
    flow criar(user_id: i64, produto_id: i64) -> i64;
    flow cancelar(id: i64) -> bool;
}

capsule Pagamentos {
    flow processar(valor: f64, pedido_id: i64) -> bool;
    flow estornar(id: i64) -> bool;
}
