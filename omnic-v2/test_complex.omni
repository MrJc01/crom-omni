// Test file for Phase 27: AST Agnosticism & Operator Precedence

export fn complex_test(n: i64) -> string {
    let result = (n * 2) + 10;
    if (result > 20) {
        return "Resultado alto: " + result;
    }
    return "Baixo";
}

export fn bool_test() -> bool {
    let a = true;
    let b = false;
    return a;
}

export fn string_test() -> string {
    let msg = "Hello World";
    return msg;
}
