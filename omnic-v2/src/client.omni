// Client module - compiles to JavaScript
import "shared/models.omni";

fn display_user(user: User) {
    print("User: " + user.name + " (" + user.email + ")");
}

fn main() {
    print("Client starting...");
    let user = create_user(42, "Alice", "alice@example.com");
    display_user(user);
}

main();
