// Test file for export keyword verification

export let VERSION = "1.0.0";

export fn greet(name: string) -> string {
    return "Hello, " + name;
}

export struct Person {
    name: string,
    age: i64
}

fn internal_helper() -> i64 {
    return 42;
}
