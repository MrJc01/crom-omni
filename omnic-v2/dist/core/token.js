
// Generated by Omni Compiler
'use strict';


const TOKEN_EOF = 0;

const TOKEN_ILLEGAL = 1;

const TOKEN_IDENTIFIER = 10;

const TOKEN_INT = 11;

const TOKEN_STRING = 12;

const TOKEN_ASSIGN = 20;

const TOKEN_PLUS = 21;

const TOKEN_MINUS = 22;

const TOKEN_BANG = 23;

const TOKEN_ASTERISK = 24;

const TOKEN_SLASH = 25;

const TOKEN_LT = 26;

const TOKEN_GT = 27;

const TOKEN_EQ = 28;

const TOKEN_NOT_EQ = 29;

const TOKEN_COLON = 30;

const TOKEN_DOT = 31;

const TOKEN_COMMA = 40;

const TOKEN_SEMICOLON = 41;

const TOKEN_LPAREN = 42;

const TOKEN_RPAREN = 43;

const TOKEN_LBRACE = 44;

const TOKEN_RBRACE = 45;

const TOKEN_LBRACKET = 46;

const TOKEN_RBRACKET = 47;

const TOKEN_FN = 60;

const TOKEN_LET = 61;

const TOKEN_TRUE = 62;

const TOKEN_FALSE = 63;

const TOKEN_IF = 64;

const TOKEN_ELSE = 65;

const TOKEN_RETURN = 66;

const TOKEN_WHILE = 67;

const TOKEN_STRUCT = 70;

const TOKEN_NATIVE = 80;

const TOKEN_IMPORT = 90;

class Token {
    constructor(data = {}) {
        this.kind = data.kind;
        this.lexeme = data.lexeme;
        this.line = data.line;
        this.start = data.start;
        this.end = data.end;
    }
}

function new_token(kind, lexeme, line) {
    return new Token({ kind: kind, lexeme: lexeme, line: line, start: 0, end: 0 });
}






module.exports = { TOKEN_EOF, TOKEN_ILLEGAL, TOKEN_IDENTIFIER, TOKEN_INT, TOKEN_STRING, TOKEN_ASSIGN, TOKEN_PLUS, TOKEN_MINUS, TOKEN_BANG, TOKEN_ASTERISK, TOKEN_SLASH, TOKEN_LT, TOKEN_GT, TOKEN_EQ, TOKEN_NOT_EQ, TOKEN_COLON, TOKEN_DOT, TOKEN_COMMA, TOKEN_SEMICOLON, TOKEN_LPAREN, TOKEN_RPAREN, TOKEN_LBRACE, TOKEN_RBRACE, TOKEN_LBRACKET, TOKEN_RBRACKET, TOKEN_FN, TOKEN_LET, TOKEN_TRUE, TOKEN_FALSE, TOKEN_IF, TOKEN_ELSE, TOKEN_RETURN, TOKEN_WHILE, TOKEN_STRUCT, TOKEN_NATIVE, TOKEN_IMPORT, new_token, Token };
